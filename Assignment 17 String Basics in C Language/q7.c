//Write a program in C to count the total number of alphabets, digits and special
//characters in a string.
int main(int argc, char const *argv[])
{
    int i,ch,num,sp;
    char c[100];
    printf("Enter a string. : ");
    scanf("%[^\n]%*c",c);
    for(i=0;c[i];i++)if((str>65 &&str<) || (str> && str<))ch++;
    else if(str> && str<)num++;
    else sp;
    printf("character : %u\n",ch);
    printf("number : %u\n",num);
    printf("special character : %u\n",sp);
    return 0;
}
